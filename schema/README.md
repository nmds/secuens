# Schema

This directory will contain a JSON Schema for CueScript metadata validation, to support tooling that needs to validate metadata keys and values programmatically.

Planned for a future release. If you're building a tool that validates CueScript metadata, open an Issue.
